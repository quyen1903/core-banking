spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:${CIF_TEST_DB_PORT}/cif_refactor_test
    username: cif_test
    password: local-only
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
logging:
  level:
    org.hibernate.orm.jdbc.bind: OFF
